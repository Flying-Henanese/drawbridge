# Drawbridge

Drawbridge is a guarded MCP bridge for remote deployment and runtime observation.

The implementation follows `TECHNICAL_DESIGN.md` and `MVP_IMPLEMENTATION_SPEC.md`.
See `docs/OPERATIONS.md` for the local and server deployment workflow.
