# Drawbridge demo application

This is a small Compose input used by the local validation workflow. It is intentionally
not a production application and does not contain a Git history; the test suite creates
an isolated Git repository around an equivalent fixture.
